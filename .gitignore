# Photo originals — 111 MB, EXIF/GPS intact, must never ship. Serve assets/photos/web/ instead.
assets/photos/originals/

# Scratch tarballs / staging from the content-prep pass — not part of the project.
_to_delete/

# Raw Google Drive source material (173 MB of docx/PDF/photo originals) — kept local only.
Documents/

# Local-only internal infra runbook (Gitea/Caddy/Mac Mini) — kept out of the public repo.
docs/INFRA-LOCAL.md
*.local.md

# OS / editor cruft
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node (once a framework is chosen)
node_modules/
dist/
build/
.env
.env.local
*.log
